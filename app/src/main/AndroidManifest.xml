<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.digicorp.easymenu">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label=""
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".UI.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.LoginActivity" />
        <activity android:name=".UI.SignUpActivity" />
        <activity android:name=".UI.OrderList" />
        <activity android:name=".UI.CreateOrderActivity" />
        <activity android:name=".UI.CreateOrderItem" />
        <activity android:name=".UI.OrderItemType" />
        <activity android:name=".UI.ListItemsActivity" />

    </application>

</manifest>